generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Project {
  id        String     @id @default(cuid())
  ownerId   String
  title     String     @default("My Journey")
  waypoints Waypoint[]
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt

  @@index([ownerId, updatedAt])
}

/// One row per accepted AI generation. Rate limiting counts these rather than
/// keeping state in memory, because serverless instances do not share memory
/// and a per-instance counter would let N instances allow N times the budget.
model AiRequest {
  id        String   @id @default(cuid())
  ownerId   String
  createdAt DateTime @default(now())

  @@index([ownerId, createdAt])
  @@index([createdAt])
}

model Waypoint {
  id        String   @id @default(cuid())
  projectId String
  project   Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  order     Int
  name      String
  lat       Float
  lng       Float
  transport String   @default("plane")
  emoji     String?

  @@index([projectId, order])
}
